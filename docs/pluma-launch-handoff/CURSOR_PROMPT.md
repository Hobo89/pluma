Implement the Pluma launch corrections in this website repository. The handoff is in `docs/pluma-launch-handoff/`.

Read repository instructions, then `MASTER_CODING_AGENT_BRIEF.md` and `HUMAN_REVIEW_REQUIRED.md` in that folder. Read the historical comparison when mapping F01–F24; use the audit for observation detail. If a completion report already exists, inspect the current diff and resume from its verified results.

Your deliverable is a code patch and evidence, not another proposed plan. Preserve the live hero's warm ivory/peach visual direction and video-to-feather-to-photo sequence. Make first-load behaviour reliable, then align the rest of the site with that design and fix booking, responsive layout and language defects.

Before editing:
- Establish the repository, current branch/commit, working-tree changes, framework, package manager and supported build/test commands. Preserve unrelated changes. Do not reset, overwrite or commit them.
- Identify the real hero, navigation/theme, translations, booking adapter, page routes and policy content. Map P1–P7 to actual source files in the completion report. Avoid generated bundles and unrelated prototypes.
- Treat the September reports as dated evidence. The hero glitch is owner-reported; the local prototype's suspected causes are hypotheses until confirmed in this source. Reproduce where possible and record limitations honestly.
- If this is not the actual website source, stop source edits and request the correct checkout. Do not manufacture a replacement website.

Execute P1 → P2 → P3 → P4 → P5 → P6 → P7 from the master brief. Use its detailed requirements and acceptance checks. Adapt the implementation to the existing architecture; do not introduce a framework migration or unnecessary dependency. Validate each coherent patch before moving on. Do not stop after planning or only fixing the hero.

Keep these boundaries:
- Apply the specified design, copy and deterministic code corrections without asking the owner to choose them again.
- Never invent qualifications, legal identity, payment/cancellation rules, addresses, privacy assurances or provider configuration. Human decisions H01–H10 remain pending until answered. Mark only their dependent work as awaiting that ID and continue independent work.
- Record remote Cal/account or hosting changes separately if they cannot be made from this repository. Do not imitate a remote fix with misleading local copy.
- No deployment, live bookings, purchases, form/message submission or publication of new legal promises. Stop booking checks before submission. Do not request or expose credentials.
- Preserve verified service durations/prices, real imagery, accessibility fixes and practical visit information. Do not mark the website launch-ready merely because the build passes.

Verification must include the master's responsive, keyboard, English/Spanish and booking checks. For the hero, distinguish uncached first loads from refreshes; test every scene, fallback/error paths and reduced motion. A working poster fallback is not proof of successful video playback. Report browser/device emulation accurately; do not claim physical-device testing without it. If browser or tooling access fails, continue safe code work and mark affected checks unverified, with the exact remaining check.

Create `docs/pluma-launch-handoff/PATCH_COMPLETION_REPORT.md` using the adjacent template. Keep it current at phase boundaries so another session can resume. Reconcile every F01–F24 and new finding N01–N06 from the master brief. Include source files changed, commands and results, evidence paths, human dependencies and remaining failures. Keep fixes that already pass; do not redo them just to create a diff. In the final response, separate code completed, tests performed, unverified checks and human decisions still preventing launch.
