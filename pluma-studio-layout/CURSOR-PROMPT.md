Apply the attached Pluma studio layout design system to this project.

Read `pluma-studio-layout/DESIGN-SYSTEM.md`, `pluma-studio-layout/tokens.json`, and `.cursor/rules/pluma-studio-layout.mdc` before editing. Inspect the existing framework, routes, content, styles, and reusable components. Keep existing working integrations and follow the project's other instructions.

Use `src/styles/pluma-studio.css` as the source of truth for tokens and reusable patterns. Wrap the styled page in `.psl`. Build a white, editorial wellness layout with narrow regular serif headings (Parkinsans), forest-green type, lime pill actions, rounded natural-light photography, wide whitespace, and a centered narrow content column. Preserve the full section sequence: photographic hero → collection/treatments → alternating feature stories → asymmetric benefits mosaic → quote/collage → promotional strip → testimonials → FAQ → portrait strip → booking CTA → dark green footer.

Retain the project's own branding and truthful content. Adapt products into treatments and use real service descriptions and booking details. Do not copy external brand names, review counts, outcome claims, or product prices. Use licensed photos; never use full-page reference screenshots as production assets. If approved content or assets are missing, expose clearly labeled placeholders and report what is needed.

Use the proposed responsive behavior in the specification. Do not reproduce faded sections, hidden meaningful content, or blank animation gaps from reference screenshots. Keep body copy readable, ensure accessible controls and image contrast, and honor reduced motion. Load Parkinsans for display typography and Instrument Sans for body/UI.

Validate desktop and phone layouts, check keyboard navigation and real integrations, and report changes plus anything still unconnected. Do not add dependencies or migrate frameworks just to apply the visual system.
