/**
 * Previously preloaded Cal.com on every route. Booking now loads Cal only when
 * the app-owned dialog or /book page mounts an embed (booking intent).
 */
export function CalModalBootstrap() {
  return null;
}
